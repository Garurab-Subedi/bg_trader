// list of Enums
// They cannot be created inside the class

enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipped, deliverd }

enum PaymentMethods { esewa, khalti, visa, mastercard, creditard }
